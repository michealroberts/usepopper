import {
  describe,
  expect,
  it
} from '@jest/globals'

import {
  usePopper
} from './'

describe('usePopper', () => {
  it('Should Be Defined', () => {
    expect(usePopper).toBeDefined()
  })
})